schema_version: "1.1"
name: review-tests-no-false-positives
description: >
  Precision twin of review-tests-finds-violations, on a suite whose tests are all
  sound. Needs --allow-tools Bash to run (the agent executes the suite).
context:
  scaffold_script: fixture.sh
  add_dirs: [fixtures, .]
