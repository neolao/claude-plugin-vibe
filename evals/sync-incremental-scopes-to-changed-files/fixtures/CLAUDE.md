# CLAUDE.md — fixture-app

## Project overview

Fixture app for the `/vibe:sync` eval — a tiny Node.js service with two
independent source modules.

## Architecture

Source directories: `src/auth/`, `src/billing/`.
Stack: plain JavaScript (Node.js), no framework, no build step.

@.vibe/index.md
