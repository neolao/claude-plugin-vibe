# Glossary

## Workspace
The top-level container a user's account belongs to. All content a user
creates (documents, uploads, comments) is stored inside exactly one
Workspace; a user account cannot belong to more than one Workspace at a
time in the current data model.

_Sources:_ `src/models/workspace.md`
