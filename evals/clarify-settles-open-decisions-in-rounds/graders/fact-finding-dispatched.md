---
type: tool_used
tool: Agent
input_match: "general-purpose"
min: 1
max: 999
weight: 2
---

"Fact-finding is never the user's job": how a user's data is scoped
(the Workspace fact in `.vibe/glossary.md`) is answerable by looking, so
it must be found by dispatching a `general-purpose` sub-agent (Agent tool),
not asked to the user as if it were an open product decision.
