# CLAUDE.md — tiny-greeter

## Project overview

A minimal Python command-line utility that prints a greeting. No web server,
no ORM, no classes, no domain layer.

## Development workflow

Human review only; no automated test suite and no dependency manifest.

## Constraints

- Keep the script dependency-free (standard library only).
